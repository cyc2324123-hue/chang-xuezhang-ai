# 发布与更新说明

## 第一次发布

1. 创建一个公开GitHub仓库，例如 `chang-xuezhang-ai`。
2. 把本发布包内的全部内容上传到仓库的 `main` 分支。
3. 打开仓库 `Settings → Pages`。
4. 选择 `Deploy from a branch`。
5. 选择 `main` 和 `/docs`，保存。
6. 等待GitHub生成公开网址。

## 日常更新

开发工程完成修改与验收后，重新生成本发布包，再把变更推送到同一仓库。公开网址不变。

## 不要放进公开仓库的内容

- 个人身份证明、账号密码和API密钥
- 内部知识库、协作画像和项目指令
- 用户对话日志和未脱敏材料
- 未确认版权或未获得发布授权的素材

## 当前网站特征

- 纯静态网站，不依赖传统后端服务器
- 72个有来源、带适用范围的高频问题
- 6类各12题，默认展示“入学前”问题
- 校园实拍、官方入口、即时回答和微信人工承接
- 手机、平板和桌面浏览器自适应
